apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    uid: securerag-loki
    url: http://localhost:3100
    isDefault: true
    editable: false
    jsonData:
      maxLines: 2000
